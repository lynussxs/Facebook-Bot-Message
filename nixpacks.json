{
  "phases": {
    "setup": {
      "nixPkgs": ["python3", "python3Packages.pip", "gcc"]
    },
    "install": {
      "cmds": ["pip install -r messenger-bot/requirements.txt --break-system-packages"]
    },
    "build": {
      "cmds": ["echo 'Skipping build phase for Python'"]
    }
  },
  "start": {
    "cmd": "python messenger-bot/bot.py"
  }
}
