{
  "phases": {
    "setup": {
      "nixPkgs": ["python3", "python3Packages.pip", "gcc"]
    },
    "install": {
      "cmds": ["pip install -r messenger-bot/requirements.txt"]
    }
  },
  "start": {
    "cmd": "python messenger-bot/bot.py"
  }
}
